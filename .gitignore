# dependencies
node_modules

# next.js
.next
out

# production
build
dist

# env files — never commit secrets
.env
.env.local
.env.*.local
.env.production

# app data (user accounts, password hashes, sessions) — never commit
data/

# misc
*.log
.DS_Store
*.tsbuildinfo
.freebuff/
